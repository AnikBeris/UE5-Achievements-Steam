
# 🎮 История о том, как я настраиваю достижения, статистику и таблицы лидеров в Steamworks


[⬆️ Наверх](#-содержание)
## 📊 1. Настройка статистики

<details> 
    <summary>⚙️ Развернуть описание</summary>

![](media/Tutorial/stateboard.png)

Название статистики должно начинаться с `Stat_`  
Рекомендуется числовое имя и сортировка по убыванию (но не обязательно).


- При создании статистики вы увидети следующие графы:
  - **ID** — автоматически генерируемый номер для каждого достижения.
  - **Type** — INT, FLOAT или AVGRATE.
  - - **INT** — 32-битное целое число со знаком (к примеру, число сыгранных игр).
  - - **FLOAT** — 32-битное число с плавающей точкой (к примеру, число пройденных автомобилем миль).
  - - **AVGRATE** — скользящее среднее. См. раздел [«AVGRATE-статистика»](https://partner.steamgames.com/doc/features/achievements#AVGRATE)
  - **Название для API** — строка, используемая API при доступе к этой статистике.
  - **Установлено** — показывает, кто может задавать статистику. По умолчанию — клиент.
  - **Только приращение** — если установлено, этой статистике позволено только увеличиваться с течением времени.
  - **Максимальное изменение** — если установлено, накладывает ограничение на изменение значения в результате одного вызова SetStat.
  - **Минимальное значение** — если установлено, определяет минимум для значения статистики. По умолчанию равно минимуму для соответствующего типа числа (INT_MIN или -FLT_MAX).
  - **Максимальное значение** — если установлено, определяет максимум для значения статистики. По умолчанию равно максимуму для соответствующего типа числа (INT_MAX или FLT_MAX).
  - **Стандартное значение** — если установлено, новый пользователь получит это значение по умолчанию для данной статистики. Если не установлено, равно нулю.
  - **Суммарное** — если установлено, Steam будет хранить общее суммарное значение для этой статистики. См. ниже раздел «Общие статистики».
  - **Название для отображения** — это название отображается в вашем приложении.

Статистики AVGRATE могут обладать дополнительными свойствами:
  - **Окно** — размер скользящего окна, используемого для усреднения.
Статистика типа AVGRATE автоматически усредняется. См. раздел [«AVGRATE-статистика»](https://partner.steamgames.com/doc/features/achievements#AVGRATE) ниже.

</details>

---

[⬆️ Наверх](#-содержание)
## 🏆 2. Создание таблицы лидеров (Leaderboard)

<details>
    <summary>⚙️ Развернуть описание</summary>

![](./media/Tutorial/Leaderboard.png)

- Следующие поля должны быть заполнены при создании таблицы лидеров:
  - **Название** - Используйте то, которое имеет смысл с точки зрения разработки.
  - **Название в сообществе** - Если таблица лидеров будет отображаться в центре сообщества, задайте здесь общедоступное название. Если ничего не ввести, таблица лидеров не появится в центре сообщества.
  - **Метод сортировки** - Выберите подходящий для данной таблицы лидеров: для таблиц, основанных на месте игрока, используйте метод «По возрастанию», а для основанных на счёте — «По убыванию».
  - **Тип отображения** - Определяет тип данных, показываемых с таблицей лидеров. Варианты: числа, секунды или миллисекунды.
  - **Запись данных** - Если установить значение «Доверенная», клиенты не смогут отправлять данные об очках в таблицы лидеров, это можно будет сделать только с помощью веб-API SetLeaderboardScore. Стандартное значение — false.
  - **Считывание данных** - Если установить значение «Друзья», игра сможет считывать данные таблицы лидеров только для друзей пользователя, но полные данные таблицы всегда можно будет прочесть с помощью веб-API. Стандартное значение — false.

```info
> ⚠️ Без заполненного поля `Название сообщества` таблица не появится в Steam Community.
```

</details>

---

[⬆️ Наверх](#-содержание)
## 🥇 3. Настройка достижений (Achievements)

<details>
    <summary>⚙️ Развернуть описание</summary>

![](media/Tutorial/Achievements.png)

- Достижения обладают следующими свойствами:
  - **ID** — автоматически генерируемый номер для каждого достижения.
  - **Название для API** — строка, используемая API при доступе к этому достижению.
  - **Прогресс статистики** — отсылает к статистике, используемой в сообществе как индикатор прогресса для этого достижения. Достижение автоматически разблокируется, если статистика достигает значения разблокировки.
  - **Название для отображения** — это название показывается в уведомлениях клиента и в сообществе. Может быть локализовано.
  - **Описание** — описание достижения, которое показывается в сообществе. Может быть локализовано.
  - **Установлено** — показывает, кто может разблокировать достижение. По умолчанию — клиент. Больше информации здесь.
  - **Скрытое?** — если установлено, «скрытое» достижение (совсем) не показывается на странице пользователя в сообществе, пока он не разблокирует его.
  - **Иконка полученного** — показывается, когда достижение получено.
  - **Иконка неполученного** — показывается до тех пор, пока достижение не получено.

```info
> ⚠️ ОГРАНИЧЕНИЯ
>  Названия и иконки достижений должны подходить всем возрастам.
>  По умолчанию изначально в играх не может быть более 100 достижений. Когда приложению станут доступны функции профиля, вы сможете добавить новые достижения.
```

---

- В разделе `« Прогресс статистики »` из выподающего меню выберети нужную Вам статистику, которую делали ранее.
  
![](media/Tutorial/Achievements_settings.png)

![](media/Tutorial/Achievements_settings_2.png)

---

## После настройки — не забудьте **опубликовать изменения** в Steamworks.

![](media/Tutorial/addNewFile.png)

</details>

---

[⬆️ Наверх](#-содержание)
## 🧠 4. Чертёж логики (Blueprint)

Чтобы избежать потери данных **рекомендую**:
- Разместить логику в **GameInstance**.
- Не используйте **Sequence** так как события асинхронны часто возникают проблемы.
- Кроме того, события являются `асинхронными`, поэтому они должны находиться в `Event Graph`, не стоит их пихать в функции.
  
> 💡 **ПЛЮС** - к всему празнику можно получить доступ отовсюду.
> 💡 **МИНУС** - `GameInstance` имеет свойство разбухать до очень неприличных значений.

Так, что создаём `GameInstance` и вперёд к следующему шагу.

---

[⬆️ Наверх](#-содержание)
## 📈 5. Чтение статистики

<details>
    <summary>⚙️ Развернуть описание</summary>

- В `GameInstanc` на `Event BeginPlay` добавьте **Delay(1s)** перед обращением к Steam. *(без `Delay` быают проскальзования инициализации)*
- Сохраняйте полученные значения в **SaveGame**.
  
Тут мы по ивенту считываем текущии значения статистики из `Steam` и сохраняем их в зарание подготовленые переменные в сохранении игры. 

**ПЛЮС** - нам не нужно постоянно дёргать `Steam` чтобы считывать состояния таблицы лидеров *(это связано с тем, что считывание данных таблицы лидеров занимает время, поэтому вызывает трудность делать это на лету. Так же нет контроля над результатом считывания данных успешно или нет, а может скачено частично. Узнаем когда статистика просто не запустится)*. Поэтому мы используем его один раз при каждом запуске игры, этого достаточно.

**МИНУС** - требует написания даполнительной логики

```info
Что бы не словить очередную ошибку не стоит использовать для этого `sequence`. Строим паравозик)))
```

</details>

---

[⬆️ Наверх](#-содержание)
## 🧮 6. Работа со статистикой

<details>
    <summary>⚙️ Развернуть описание</summary>


## 📥 Добавление статистики в сохранение

Если вы хотите добавить статистику, сначала запихните данные в `сохранение игры`.

![](media/Tutorial/Reading_Stats.png)


## ✏️ Изменение статистики

![](media/Tutorial/Saving_Stats.png)
(это только пример)


## 📤 Загрузка статистики
Затем пришло время загрузить статистику следующим образом.

Не используйте для этого последовательность , так как это приведёт к сбою . Для корректной работы необходимо подключить её, как указано выше. Также убедитесь, что вы НЕ вызываете это событие несколько раз до его завершения. Поэтому лучше всего указать определённую точку, где оно вызывается один раз (например, конец уровня).

ещё раз

#⚠️ Не вызывайте загрузку несколько раз подряд.
# Делайте это в определённой точке (например, конец уровня).

![](media/Tutorial/Uploading_Stats.png)



## Результат:

Возможно, вам придется выйти из игры и дать Steam немного времени, что бы он обновился, прежде чем данные и сам прогресс отобразится

![](media/Tutorial/Result.png)
Когда вы достигнете своей цели, достижение автоматически разблокируется.

</details>

---

[⬆️ Наверх](#-содержание)
## 🔁 7. Сброс статистики

<details>
    <summary>⚙️ Развернуть описание</summary>

есть несколько способов сбросить статистику, но в `Blueprint` не так много хороших способов.

## 🧹 Способ 1 — через Steamworks

Удалите запись через:

```
Steamworks → Таблицы лидеров → Просмотреть результаты → Удалить
```

замечу, что статистика не будет сброшена в самой учетной записе. Только глобально отображение.

## 🧩 Способ 2 — через консоль Steam

Откройте консоль:

`( Win + R )`

введите следующее

```sh
steam://open/console
```

![](media/Tutorial/Steam_Console.png)


# Команды:

## Сбросить все достижения:

```sh
reset_all_stats 480
```
- (где 480 — ваш уникальный AppID игры ).

## Сбросить конкретное достижение:

```sh
achievement_clear 480 Achievement_Name
```

## Так же можно приписать значения в ярлык, что бы не прописывать постоянно команду

```sh
 - steam://open/console
```

![](media/Tutorial/Steam_Console_icon_1.png)

</details>

---
---

[⬆️ Наверх](#-содержание)
# 📊 8. Подключение Steamworks (Steam) к Unreal Engine 5
<details> 
    <summary>⚙️ Развернуть описание</summary>
    
- Переходим в деректорию вашего прокта а именно в папку `Config`

- Откройте `DefaultGame.ini`, прокрутите вниз и вставьте строки для интеграции `Steam`:

```ini
[/Script/Engine.GameEngine]
; Используем Steam как сетевой драйвер (важно для мультиплеера)
+NetDriverDefinitions=(DefName="GameNetDriver",DriverClassName="OnlineSubsystemSteam.SteamNetDriver",DriverClassNameFallback="OnlineSubsystemUtils.IpNetDriver")

[OnlineSubsystem]
DefaultPlatformService=Steam

[OnlineSubsystemSteam]
bEnabled=true
;VK_Play
;SteamDevAppId={ID_VK_Play}
;Steam
SteamDevAppId=480
bInitServerOnClient=true
```

- Далее нужно добавить достижения (ачивки), которые мы создадим ниже. Пример будет выглядеть так:

![](media/Tutorial/Achievements.png)

```ini
Achievement_0_Id=AB_Level_1
Achievement_1_Id=AB_Level_2
Achievement_2_Id=AB_Level_3
Achievement_3_Id=AB_AMMO_1000
Achievement_4_Id=AB_AMMO_10000
Achievement_5_Id=AB_AMMO_100000
Achievement_6_Id=AB_AMMO_1000000
Achievement_7_Id=AB_BOOTS
Achievement_8_Id=AB_GLADIATOR
Achievement_9_Id=AB_ALMERS
Achievement_10_Id=AB_HANDIN
Achievement_11_Id=AB_1_Start
Achievement_12_Id=AB_BIG_BOSS
Achievement_13_Id=AB_1_RADIO_POP
Achievement_14_Id=AB_Secret_room
```

- Полный скрипт будет выглядеть так. Где вам нужно заменить тестовое значение `480` в `SteamDevAppId=480` на id вашей игры в кабинете `Steamworks`. Без этого игра будет работать на тестовом `id 480` от Steam

```ini
[/Script/Engine.GameEngine]
; Используем Steam как сетевой драйвер (важно для мультиплеера)
+NetDriverDefinitions=(DefName="GameNetDriver",DriverClassName="OnlineSubsystemSteam.SteamNetDriver",DriverClassNameFallback="OnlineSubsystemUtils.IpNetDriver")

[OnlineSubsystem]
DefaultPlatformService=Steam

[OnlineSubsystemSteam]
bEnabled=true
SteamDevAppId=480
; bInitServerOnClient=true
Achievement_0_Id=AB_Level_1
Achievement_1_Id=AB_Level_2
Achievement_2_Id=AB_Level_3
Achievement_3_Id=AB_AMMO_1000
Achievement_4_Id=AB_AMMO_10000
Achievement_5_Id=AB_AMMO_100000
Achievement_6_Id=AB_AMMO_1000000
Achievement_7_Id=AB_BOOTS
Achievement_8_Id=AB_GLADIATOR
Achievement_9_Id=AB_ALMERS
Achievement_10_Id=AB_HANDIN
Achievement_11_Id=AB_1_Start
Achievement_12_Id=AB_BIG_BOSS
Achievement_13_Id=AB_1_RADIO_POP
Achievement_14_Id=AB_Secret_room
```

---


- Вот конфиг который я использую есле нужно подкинуть [Advanced Sessions Plugin](https://vreue4.com/advanced-sessions-binaries)
  
- теперь по коду:
  
### 🔹 [/Script/Engine.GameEngine]

```ini
+NetDriverDefinitions=(DefName="GameNetDriver",DriverClassName="OnlineSubsystemSteam.SteamNetDriver",DriverClassNameFallback="OnlineSubsystemUtils.IpNetDriver")
```
- Эта строка нужна для того, чтобы движок использовал `SteamNetDriver` вместо стандартного `IpNetDriver`.
Параметр `DriverClassNameFallback` гарантирует, что если `Steam` не инициализируется, игра не упадёт, а вернётся к обычной сетевой подсистеме.

### 🔹 [OnlineSubsystem]

```ini
DefaultPlatformService=Steam
```
- Указывает UE5, что по умолчанию используется Steam, как платформа для онлайн-подсистемы

### 🔹 [OnlineSubsystemSteam]

```ini
bEnabled=true
;VK_Play
;SteamDevAppId={ID_VK_Play}
;Steam
SteamDevAppId={ID_Steam}
;bInitServerOnClient=true
```
- bEnabled=true — включает подсистему.
- SteamDevAppId должен совпадать с AppID твоего проекта в Steamworks (временно можно ставить 480 для тестов).
- bInitServerOnClient=true часто требуется для peer-host игр (если сервер запускается на клиенте).

### 🔹 Achievement_#_Id=
- Если ID в коде совпадают с ID достижений, созданных в Steamworks в разделе Achievements то Unreal подтянет их автоматически.

### 🔹 [/Script/OnlineSubsystemSteam.SteamNetDriver]

```ini
NetConnectionClassName=OnlineSubsystemSteam.SteamNetConnection
```
- Без этого сетевые подключения Steam могут не инициализироваться или инициализироваться не правильно.

```ini
; Подключение Advanced Sessions Plugin
[/Script/Engine.GameEngine]
+NetDriverDefinitions=(DefName="GameNetDriver",DriverClassName="OnlineSubsystemSteam.SteamNetDriver",DriverClassNameFallback="OnlineSubsystemUtils.IpNetDriver")

[OnlineSubsystem]
DefaultPlatformService=Steam

[OnlineSubsystemSteam]
bEnabled=true
;VK_Play
;SteamDevAppId={ID_VK_Play}
;Steam
SteamDevAppId={ID_Steam}
bInitServerOnClient=true

Achievement_0_Id=AB_Level_1
Achievement_1_Id=AB_Level_2
Achievement_2_Id=AB_Level_3
Achievement_3_Id=AB_AMMO_1000
Achievement_4_Id=AB_AMMO_10000
Achievement_5_Id=AB_AMMO_100000
Achievement_6_Id=AB_AMMO_1000000
Achievement_7_Id=AB_BOOTS
Achievement_8_Id=AB_GLADIATOR
Achievement_9_Id=AB_ALMERS
Achievement_10_Id=AB_HANDIN
Achievement_11_Id=AB_1_Start
Achievement_12_Id=AB_BIG_BOSS
Achievement_13_Id=AB_1_RADIO_POP
Achievement_14_Id=AB_Secret_room

[/Script/OnlineSubsystemSteam.SteamNetDriver]
NetConnectionClassName=OnlineSubsystemSteam.SteamNetConnection
```

### Вот пример рабочих конфигов для `OnlineSubsystemSteam` от `GPT`

```
[OnlineSubsystemSteam]
; === Основные параметры ===
bEnabled=true                        ; Включает Steam Subsystem
SteamDevAppId=480                    ; ID тестовой игры (замени на свой AppID)
SteamAppId=480                       ; Продублируй для стабильности
bInitServerOnClient=true             ; Инициализировать сервер на клиенте (нужно для Listen Server)
bRelaunchInSteam=true                ; Перезапускать игру под Steam, если запущена напрямую
bUseSteamNetworking=true             ; Использовать Steam P2P (замена IpNetDriver для матчмейкинга)
bAllowP2PPacketRelay=true            ; Разрешить ретрансляцию через Steam Relay (важно для NAT)
P2PConnectionTimeout=90              ; Таймаут P2P-соединения в секундах

; === Overlay и интерфейс ===
bOverlayEnabled=true                 ; Включает Steam Overlay
bVACEnabled=false                    ; Включает/отключает VAC (античит)
bAntiCheatEnabled=false              ; Альтернативное поле (UE 5.4+)
GameServerQueryPort=27015            ; Порт для Steam server browser (должен быть открыт)
GameServerGamePort=7777              ; Порт игрового соединения
GameServerSteamPort=8766             ; Порт Steam-сервера

; === Достижения и статистика ===
bUseAchievements=true                ; Включает систему достижений Steam
bUseStats=true                       ; Включает систему статистики Steam
bUseUserCloud=true                   ; Включает Steam Cloud для сохранений
bUsePresence=true                    ; Включает показ статуса игрока в Steam (играет, в меню и т.д.)

; === Матчмейкинг и сессии ===
bAllowJoinViaPresence=true           ; Разрешить присоединение через статус в Steam
bAllowJoinViaPresenceFriendsOnly=false ; Только друзья могут подключаться
bAllowJoinInProgress=true            ; Разрешить присоединение во время игры
bUseLobbiesIfAvailable=true          ; Использовать Steam Lobbies для матчей (Listen Server)
bSteamMatchmaking=true               ; Включает поддержку Steam Matchmaking API
bSupportsSessionCreation=true        ; Разрешить создание сессий

; === Поддержка Dedicated Server ===
bEnableServerLogging=true            ; Включить логирование Steam-сервера
bVACSecured=false                    ; VAC защита для сервера
bUseGameServerAuth=true              ; Аутентификация Steam сервера через логин
bAllowSteamServerLogin=true          ; Разрешить авторизацию сервера под Steam аккаунтом

; === Разное ===
GameVersion=1.0.0.0                  ; Версия игры, отображаемая в Steam
bDebugServerAvailability=false       ; Для отладки проблем подключения
bUseP2PNetworking=true               ; Использовать Peer-to-Peer
bUseSteamNetworkingSockets=true      ; Новая система сетевого транспорта (SteamNetworkingSockets)
bEnableSteamVoice=true               ; Включает голосовой чат Steam
bUseSteamAuthentication=true         ; Проверка подлинности пользователей через Steam

; === Пример достижений (можно убрать, если они в отдельной секции) ===
Achievement_0_Id=AB_Level_1
Achievement_1_Id=AB_Level_2
Achievement_2_Id=AB_Level_3
Achievement_3_Id=AB_AMMO_1000
Achievement_4_Id=AB_AMMO_10000
Achievement_5_Id=AB_AMMO_100000
Achievement_6_Id=AB_AMMO_1000000
Achievement_7_Id=AB_BOOTS
Achievement_8_Id=AB_GLADIATOR
Achievement_9_Id=AB_ALMERS
Achievement_10_Id=AB_HANDIN
Achievement_11_Id=AB_1_Start
Achievement_12_Id=AB_BIG_BOSS
Achievement_13_Id=AB_1_RADIO_POP
Achievement_14_Id=AB_Secret_room

```

| Параметр                         | Назначение                                      | Рекомендация                            |
| -------------------------------- | ----------------------------------------------- | --------------------------------------- |
| `SteamDevAppId` / `SteamAppId`   | Уникальный ID игры в Steamworks                 | Всегда дублируй оба                     |
| `bInitServerOnClient`            | Позволяет клиенту запускать сессию как хост     | Для listen-серверов — `true`            |
| `bRelaunchInSteam`               | Перезапускает игру под Steam                    | `true`, иначе Overlay не всегда активен |
| `bAllowP2PPacketRelay`           | Использовать Steam Relay, если NAT              | Лучше `true` для P2P                    |
| `bUseSteamNetworkingSockets`     | Новый API от Valve (замена старого P2P)         | Рекомендуется включить                  |
| `bUsePresence`                   | Позволяет показывать статус игрока              | Включить, если есть мультиплеер         |
| `bUseUserCloud`                  | Синхронизация сохранений через Steam Cloud      | Включить по необходимости               |
| `bUseStats` / `bUseAchievements` | Статистика и достижения                         | Включить, если реализуешь через API     |
| `bUseLobbiesIfAvailable`         | Использовать Steam Lobbies вместо direct join   | Удобно для матчмейкинга                 |
| `bEnableSteamVoice`              | Активация голосового чата через Steam Voice API | По желанию                              |



</details>


---

[⬆️ Наверх](#-содержание)
# 💻 9. Кодовая часть (Blueprints в Unreal Engine 5)

<details>
    <summary>⚙️ Развернуть описание</summary>

## Чтобы `сбросить все достижения` из `Unreal BP`, выполните следующее:

```sh
(online.ResetAchievements)
```

![](media/Tutorial/Reset_All_Achievements.png)


Если вы отправите прогресс нескольких ачивок из UE в Steam за короткий промежуток времени 
например:
вы совершили: 100 убийств, подняли 2 камня и в конце ещё и повысили свой уровень
Вроде всё хорошо, но одновременно отправили 3 достижения на получение. Но получите только 1-ну из них, остальные будут весеть в подвешенном состоянии.

Это один из примеров системы очереди разблокировки достижений .

## В вашем `GameInstance` добавьте следующие переменные :
строковый массив , куда мы добавим идентификаторы достижений для разблокировки
, и логическое значение, чтобы сообщить нам, что процесс идет.

![](media/Tutorial/UE_BP_1.png)

Затем запишите эту систему:

![](media/Tutorial/UE_BP_2.png)

Всякий раз, когда вы вызываете `Custom Events` разблокировки ачивки, то он добавится в массив (в конец).
Затем мы проверяем, является ли элементы масива достижением `Unlocking Achievements`

Выполняем `Cache Achievements` один раз, затем проверяем длину массива. Если она равна `0`, то всё готово, и мы сбрасываем `Is_Unlocking _Achievement`. Если она `> 0`, то продолжаем, записываем ход достижения для `Array[0]`, а отом удаляем `Array[0]`. Затем повторяем этот цикл, пока не закончатся элементы в массиве.

## Тестирование

Вот как можно разблокировать достижения. Они будут открываться одно за другим.

![](media/Tutorial/UE_BP_3.png)

</details>




* * * * * * * * * * * * * * * * * * 
* * * * * * * * * * * * * * * * * * 
